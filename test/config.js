
/*
'use strict';

const path = require(`path`);

module.exports = {
  test: `1-hello-world`,
  cwd: path.resolve(path.join(__dirname, '../')),
  cmd: `node`,
  args: [`app.js`],
  port: 8081,
  msg: `Hello, world!`
};
*/